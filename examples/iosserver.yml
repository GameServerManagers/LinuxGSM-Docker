
# usage:
#  docker-compose -f ./examples/iosserver.yml up -d
volumes:
  iosserver-files:

name: lgsm

services:
  iosserver:
    image: "gameservermanagers/linuxgsm-docker:iosserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - iosserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
