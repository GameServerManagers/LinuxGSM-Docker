
# usage:
#  docker-compose -f ./examples/wfserver.yml up -d
volumes:
  wfserver-files:

name: lgsm

services:
  wfserver:
    image: "gameservermanagers/linuxgsm-docker:wfserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - wfserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
