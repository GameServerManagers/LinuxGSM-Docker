
# usage:
#  docker-compose -f ./examples/stnserver.yml up -d
volumes:
  stnserver-files:

name: lgsm

services:
  stnserver:
    image: "gameservermanagers/linuxgsm-docker:stnserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - stnserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
