
# usage:
#  docker-compose -f ./examples/bfvserver.yml up -d
volumes:
  bfvserver-files:

name: lgsm

services:
  bfvserver:
    image: "gameservermanagers/linuxgsm-docker:bfvserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - bfvserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
