
# usage:
#  docker-compose -f ./examples/roserver.yml up -d
volumes:
  roserver-files:

name: lgsm

services:
  roserver:
    image: "gameservermanagers/linuxgsm-docker:roserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
      # please fill your credentials below
      - "CONFIGFORCED_steamuser="
      - "CONFIGFORCED_steampass="
    volumes:
      - roserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
