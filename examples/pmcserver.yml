
# usage:
#  docker-compose -f ./examples/pmcserver.yml up -d
volumes:
  pmcserver-files:

name: lgsm

services:
  pmcserver:
    image: "gameservermanagers/linuxgsm-docker:pmcserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - pmcserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
