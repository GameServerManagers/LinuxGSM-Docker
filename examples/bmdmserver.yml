
# usage:
#  docker-compose -f ./examples/bmdmserver.yml up -d
volumes:
  bmdmserver-files:

name: lgsm

services:
  bmdmserver:
    image: "gameservermanagers/linuxgsm-docker:bmdmserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - bmdmserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
