
# usage:
#  docker-compose -f ./examples/loserver.yml up -d
volumes:
  loserver-files:

name: lgsm

services:
  loserver:
    image: "gameservermanagers/linuxgsm-docker:loserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - loserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
    ports:
      # Game
      - "7777:7777/udp"
      # Query
      - "27015:27015/udp"
