
# usage:
#  docker-compose -f ./examples/tfcserver.yml up -d
volumes:
  tfcserver-files:

name: lgsm

services:
  tfcserver:
    image: "gameservermanagers/linuxgsm-docker:tfcserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - tfcserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
    ports:
      # Game
      - "27015:27015/udp"
      # Client
      - "27005:27005/udp"
