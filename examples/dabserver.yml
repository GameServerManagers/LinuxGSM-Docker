
# usage:
#  docker-compose -f ./examples/dabserver.yml up -d
volumes:
  dabserver-files:

name: lgsm

services:
  dabserver:
    image: "gameservermanagers/linuxgsm-docker:dabserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - dabserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
