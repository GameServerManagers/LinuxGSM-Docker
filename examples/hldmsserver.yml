
# usage:
#  docker-compose -f ./examples/hldmsserver.yml up -d
volumes:
  hldmsserver-files:

name: lgsm

services:
  hldmsserver:
    image: "gameservermanagers/linuxgsm-docker:hldmsserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - hldmsserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
