
# usage:
#  docker-compose -f ./examples/vintsserver.yml up -d
volumes:
  vintsserver-files:

name: lgsm

services:
  vintsserver:
    image: "gameservermanagers/linuxgsm-docker:vintsserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - vintsserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
