
# usage:
#  docker-compose -f ./examples/dstserver.yml up -d
volumes:
  dstserver-files:

name: lgsm

services:
  dstserver:
    image: "gameservermanagers/linuxgsm-docker:dstserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - dstserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
