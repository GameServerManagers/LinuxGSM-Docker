
# usage:
#  docker-compose -f ./examples/coduoserver.yml up -d
volumes:
  coduoserver-files:

name: lgsm

services:
  coduoserver:
    image: "gameservermanagers/linuxgsm-docker:coduoserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - coduoserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
