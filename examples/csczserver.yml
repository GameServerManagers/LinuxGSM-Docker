
# usage:
#  docker-compose -f ./examples/csczserver.yml up -d
volumes:
  csczserver-files:

name: lgsm

services:
  csczserver:
    image: "gameservermanagers/linuxgsm-docker:csczserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - csczserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
    ports:
      # Game
      - "27015:27015/udp"
      # Client
      - "27005:27005/udp"
