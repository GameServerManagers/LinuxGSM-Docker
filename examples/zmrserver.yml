
# usage:
#  docker-compose -f ./examples/zmrserver.yml up -d
volumes:
  zmrserver-files:

name: lgsm

services:
  zmrserver:
    image: "gameservermanagers/linuxgsm-docker:zmrserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - zmrserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
