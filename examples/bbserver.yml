
# usage:
#  docker-compose -f ./examples/bbserver.yml up -d
volumes:
  bbserver-files:

name: lgsm

services:
  bbserver:
    image: "gameservermanagers/linuxgsm-docker:bbserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - bbserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
