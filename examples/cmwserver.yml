
# usage:
#  docker-compose -f ./examples/cmwserver.yml up -d
volumes:
  cmwserver-files:

name: lgsm

services:
  cmwserver:
    image: "gameservermanagers/linuxgsm-docker:cmwserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - cmwserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
