
# usage:
#  docker-compose -f ./examples/svenserver.yml up -d
volumes:
  svenserver-files:

name: lgsm

services:
  svenserver:
    image: "gameservermanagers/linuxgsm-docker:svenserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - svenserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
    ports:
      # Game
      - "27015:27015/udp"
      # Client
      - "27005:27005/udp"
