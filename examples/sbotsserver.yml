
# usage:
#  docker-compose -f ./examples/sbotsserver.yml up -d
volumes:
  sbotsserver-files:

name: lgsm

services:
  sbotsserver:
    image: "gameservermanagers/linuxgsm-docker:sbotsserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - sbotsserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
    ports:
      # Game
      - "7777:7777/udp"
      # Query
      - "27015:27015/udp"
