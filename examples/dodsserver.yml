
# usage:
#  docker-compose -f ./examples/dodsserver.yml up -d
volumes:
  dodsserver-files:

name: lgsm

services:
  dodsserver:
    image: "gameservermanagers/linuxgsm-docker:dodsserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - dodsserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
