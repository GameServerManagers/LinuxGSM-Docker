
# usage:
#  docker-compose -f ./examples/dodserver.yml up -d
volumes:
  dodserver-files:

name: lgsm

services:
  dodserver:
    image: "gameservermanagers/linuxgsm-docker:dodserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - dodserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
    ports:
      # Game
      - "27015:27015/udp"
      # Client
      - "27005:27005/udp"
