
# usage:
#  docker-compose -f ./examples/cssserver.yml up -d
volumes:
  cssserver-files:

name: lgsm

services:
  cssserver:
    image: "gameservermanagers/linuxgsm-docker:cssserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - cssserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
