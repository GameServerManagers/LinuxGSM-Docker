
# usage:
#  docker-compose -f ./examples/ecoserver.yml up -d
volumes:
  ecoserver-files:

name: lgsm

services:
  ecoserver:
    image: "gameservermanagers/linuxgsm-docker:ecoserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - ecoserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
