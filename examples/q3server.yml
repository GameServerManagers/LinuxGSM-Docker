
# usage:
#  docker-compose -f ./examples/q3server.yml up -d
volumes:
  q3server-files:

name: lgsm

services:
  q3server:
    image: "gameservermanagers/linuxgsm-docker:q3server"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - q3server-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
