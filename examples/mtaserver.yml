
# usage:
#  docker-compose -f ./examples/mtaserver.yml up -d
volumes:
  mtaserver-files:

name: lgsm

services:
  mtaserver:
    image: "gameservermanagers/linuxgsm-docker:mtaserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - mtaserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
