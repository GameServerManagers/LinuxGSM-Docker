
# usage:
#  docker-compose -f ./examples/rustserver.yml up -d
volumes:
  rustserver-files:

name: lgsm

services:
  rustserver:
    image: "gameservermanagers/linuxgsm-docker:rustserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - rustserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
