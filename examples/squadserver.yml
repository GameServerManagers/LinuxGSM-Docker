
# usage:
#  docker-compose -f ./examples/squadserver.yml up -d
volumes:
  squadserver-files:

name: lgsm

services:
  squadserver:
    image: "gameservermanagers/linuxgsm-docker:squadserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - squadserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
