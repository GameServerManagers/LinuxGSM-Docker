
# usage:
#  docker-compose -f ./examples/mhserver.yml up -d
volumes:
  mhserver-files:

name: lgsm

services:
  mhserver:
    image: "gameservermanagers/linuxgsm-docker:mhserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - mhserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
