
# usage:
#  docker-compose -f ./examples/hl2dmserver.yml up -d
volumes:
  hl2dmserver-files:

name: lgsm

services:
  hl2dmserver:
    image: "gameservermanagers/linuxgsm-docker:hl2dmserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - hl2dmserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
    ports:
      # Game
      - "27015:27015/udp"
      # Query / RCON
      - "27015:27015/tcp"
      # SourceTV
      - "27020:27020/udp"
      # Client
      - "27005:27005/udp"
