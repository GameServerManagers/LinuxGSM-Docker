
# usage:
#  docker-compose -f ./examples/btserver.yml up -d
volumes:
  btserver-files:

name: lgsm

services:
  btserver:
    image: "gameservermanagers/linuxgsm-docker:btserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - btserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
