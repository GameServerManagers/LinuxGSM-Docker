
# usage:
#  docker-compose -f ./examples/boserver.yml up -d
volumes:
  boserver-files:

name: lgsm

services:
  boserver:
    image: "gameservermanagers/linuxgsm-docker:boserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - boserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
