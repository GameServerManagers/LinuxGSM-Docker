
# usage:
#  docker-compose -f ./examples/ahlserver.yml up -d
volumes:
  ahlserver-files:

name: lgsm

services:
  ahlserver:
    image: "gameservermanagers/linuxgsm-docker:ahlserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - ahlserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
    ports:
      # Game
      - "27015:27015/udp"
      # Client
      - "27005:27005/udp"
