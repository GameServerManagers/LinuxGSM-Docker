
# usage:
#  docker-compose -f ./examples/sof2server.yml up -d
volumes:
  sof2server-files:

name: lgsm

services:
  sof2server:
    image: "gameservermanagers/linuxgsm-docker:sof2server"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - sof2server-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
