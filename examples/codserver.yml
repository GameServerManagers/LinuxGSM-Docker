
# usage:
#  docker-compose -f ./examples/codserver.yml up -d
volumes:
  codserver-files:

name: lgsm

services:
  codserver:
    image: "gameservermanagers/linuxgsm-docker:codserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - codserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
# couldn't extract ports, can only provide example how to configure it
#    ports:
#      - "27015:27015/udp"
#      - "27015:27015/tcp"
