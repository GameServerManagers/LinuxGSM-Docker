
# usage:
#  docker-compose -f ./examples/dmcserver.yml up -d
volumes:
  dmcserver-files:

name: lgsm

services:
  dmcserver:
    image: "gameservermanagers/linuxgsm-docker:dmcserver"
    tty: true
    restart: unless-stopped
    environment:
      - "CRON_update_daily=0 7 * * * update"
    volumes:
      - dmcserver-files:/home/linuxgsm
      - /etc/localtime:/etc/localtime:ro
    ports:
      # Game
      - "27015:27015/udp"
      # Client
      - "27005:27005/udp"
